<div class="notify {{level}}" id="{{id}}">
    <div class="notify-inner">
        <header>
            <h2>{{title}}</h2>
        </header>
        {{#if text }}
            <p>{{text}}</p>
        {{/if}}
    </div>
</div>